server:
  port: 9999

spring:
  jpa:
    database-platform: org.hibernate.dialect.MariaDB53Dialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB53Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: create
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/doc_test?useUnicode=true&characterEncoding=utf8
    username: lldj
    password: lldj1234